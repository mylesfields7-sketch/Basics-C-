#include <iostream>
using namespace std;

int main() {
	bool hasID = true;
	bool libraryClosed = false;

	if (hasID && !libraryClosed) {
		cout << "Access denied." << endl;
	}
	else {
		cout << "Access denied." << endl;
	}

	return 0;
}
